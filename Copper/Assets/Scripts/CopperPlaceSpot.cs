using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class CopperPlaceSpot : MonoBehaviour
{
    private bool isFull;
    public bool IsFull => isFull;
    public void SetFull()
    {
        isFull = true;
    }
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
